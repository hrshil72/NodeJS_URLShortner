<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous" />
  </head>
  <body>
    <div class="container">
      <h1>URL Shrinker</h1>

      <form action="/shortUrls" method="post" class="my-4 form-inline">
        <label class="sr-only" for="longURL">URL</label>
        <input
          class="form-control mr-2 col"
          placeholder="Enter your URL"
          type="url"
          name="longURL"
          id="longURL"
          required />
        <button class="btn btn-success" type="submit">Shrink</button>
      </form>

      <table class="table table-striped table-responsive">
        <thead>
          <tr>
            <th>Full URL</th>
            <th>Short URL</th>
            <th>Clicks</th>
          </tr>
        </thead>
        <tbody>
          <% for (let shortLink of shortUrls) { %>

          <tr>
            <td>
              <a href="<%=shortLink.longURL %>"><%=shortLink.longURL %></a>
            </td>
            <td>
              <a href="<%=shortLink.shortURL %>"><%=shortLink.shortURL %></a>
            </td>
            <td><%=shortLink.clicks %></td>
          </tr>

          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
